HOSTS:
  debian-6-x86_64:
    default_apply_opts:
      strict_variables:
    platform: debian-6-amd64
    hypervisor : docker
    docker_cmd: '["/sbin/init"]'
    image: debian:6
    docker_preserve_image: true
CONFIG:
  type: foss
  log_level: debug
